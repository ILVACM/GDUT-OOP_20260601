# 后端业务逻辑层支撑能力审查报告

## 审查基本信息
- **审查日期**：2026-06-04
- **审查范围**：Service 层业务逻辑（Controller 层不在本次审查范围）
- **审查依据**：`wiki/00-INDEX.md` 功能矩阵 + `wiki/01-Global-Standards.md` + `wiki/02-Data-Dictionary.md` + `wiki/modules/M01-M04`
- **代码路径**：`backend/src/main/java/com/cps/backend/modules/`

---

## 1. 公共基础设施审查结果：✅ 完全具备

### 已具备的功能函数
| 类名 | 方法/字段 | 说明 |
|------|-----------|------|
| `Result<T>` | `success()`/`success(T)`/`error(int, String)` | 统一 API 响应包装器，code/message/data 三字段 |
| `BusinessException` | `code` (int) + `message` (String) | 业务异常，继承 RuntimeException |
| `GlobalExceptionHandler` | `handleBusiness()`/`handleIllegalArgument()`/`handleAny()` | 三段式异常处理：BusinessException透传、IllegalArgumentException→4000、兜底→5000 |

### 符合性验证
- ✅ `Result<T>` 实现与 01-Global-Standards.md §3.1 完全一致
- ✅ 业务异常码区间划分正确（41xx用户/42xx题库/43xx考试/44xx分数/5xxx系统）
- ✅ 异常处理器不泄露堆栈信息给前端，兜底仅返回"系统繁忙"
- ✅ 全局异常处理器已标注 `@RestControllerAdvice`

---

## 2. M01 用户认证模块审查结果：⚠️ 部分具备

### 已具备的功能函数（UserService.java）

| 方法签名 | 事务控制 | 核心逻辑 |
|----------|---------|----------|
| `register(RegisterReq)` | ✅ `@Transactional` | BCrypt加密 + name唯一校验 + 默认status=1 |
| `login(LoginReq)` | —（只读） | findByName + BCrypt比对 + status检查 |
| `findById(Integer)` | —（只读） | 按id查询 + toVO转换 |
| `findByTypeAndStatus(UserType, Integer)` | —（只读） | 按角色/状态筛选 |
| `findPage(UserType, Integer, int, int)` | —（只读） | 分页查询（内存过滤实现） |
| `update(Integer, RegisterReq)` | ✅ `@Transactional` | 存在性校验 + name唯一校验 + 全量更新 |
| `changePassword(Integer, ChangePasswordReq)` | ✅ `@Transactional` | 旧密码校验 + BCrypt新密码 |
| `updateStatus(Integer, UserStatusReq)` | ✅ `@Transactional` | admin保护（最后一个不能被禁用） |
| `delete(Integer, Integer)` | ✅ `@Transactional` | 自删除保护 + 最后admin保护 |

### 发现的问题/缺失的功能

#### 问题 1：删除用户的级联检查未实现（⚠️ 中等）
- **位置**：`UserService.delete()` 第150行
- **现状**：`// TODO: 检查关联未结束考试/未批改答卷`，当前直接删除
- **规范要求**：M01-User-Auth.md §7 业务规则4 — "删除前检查未结束考试/未批改答卷；如有则禁用而非删除"
- **影响**：删除有未完成考试记录的用户会导致外键约束违反或产生孤儿数据
- **修改建议**：注入 ScoreRepository，删除前检查 `findByUser(userId)` 是否有记录，如有则改为 status=0 禁用

#### 问题 2：缺少"当前用户"查询方法（⚠️ 低）
- **现状**：无 `findByToken()` 或 `getCurrentUser()` 方法
- **规范要求**：M01-User-Auth.md §1 "当前用户信息 — 从 Token 解析并返回登录用户"
- **影响**：前端登录态保持需要此功能（可由 Controller 层从 Token 提取 userId 后调用 `findById` 实现，非硬性缺失）

#### 问题 3：注册无角色限制（⚠️ 低）
- **现状**：`register()` 允许注册任意角色（包括 admin）
- **规范要求**：M01-User-Auth.md §2 权限矩阵 — admin 拥有所有用户管理权限
- **建议**：注册接口应限制为仅 student 角色自助注册，teacher/admin 由 admin 创建

---

## 3. M02 题库管理模块审查结果：⚠️ 部分具备

### 已具备的功能函数（QuestionService.java）

| 方法签名 | 事务控制 | 核心逻辑 |
|----------|---------|----------|
| `create(QuestionCreateReq)` | ✅ `@Transactional` | answer JSON按type校验 + 初始化use/correct=0 |
| `batchCreate(List<QuestionCreateReq>)` | ✅ `@Transactional` | 批量≤100题限制 + 逐题answer校验 |
| `findById(Integer)` | —（只读） | 按id查询 + accuracy计算 |
| `search(QuestionQueryReq)` | —（只读） | 分页 + type/keyword筛选 |
| `update(Integer, QuestionUpdateReq)` | ✅ `@Transactional` | 存在性校验 + answer JSON校验 + 全量更新 |
| `delete(Integer)` | ✅ `@Transactional` | 存在性校验 + 硬删除 |
| `incrementUse(Integer)` | ✅ `@Transactional` | @Modifying use += 1 |
| `incrementCorrect(Integer)` | ✅ `@Transactional` | @Modifying correct += 1 |
| `validateAnswerJson(QuestionType, String)` | —（私有） | 5种题型多态反序列化 + 业务规则校验 |

### 发现的问题/缺失的功能

#### 问题 1：批量删除未检查题目是否被考试引用（⚠️ 高）
- **现状**：`delete()` 直接调用 `deleteById()`，无任何引用检查
- **规范要求**：M02-Question-Bank.md §8 业务规则3 — "已被纳入考试的题目被删除后，该考试仍可正常进行（因快照已固化）"
- **实际情况**：快照机制保证了历史考试不受影响，但 `question.use/correct` 统计会永久停留在已删除题目上
- **审查结论**：**当前实现符合文档规范**，但建议在删除前增加引用提示（可选）

#### 问题 2：批量删除功能缺失（⚠️ 低）
- **现状**：仅支持单题删除，无 `batchDelete(List<Integer>)` 方法
- **规范未明确要求**，但实际业务中批量删除是常见需求
- **建议**：下期补充

---

## 4. M03 考试与组卷模块审查结果：❌ 严重缺失

### 已具备的功能函数（ExamService.java）

| 方法签名 | 事务控制 | 核心逻辑 |
|----------|---------|----------|
| `createManual(ExamCreateManualReq)` | ✅ `@Transactional` | 时间窗校验 + questionId存在校验 + question_sum快照构造 + use+=1 |
| `createAuto(ExamCreateAutoReq)` | ✅ `@Transactional` | typeFilter过滤 + usePenalty加权随机 + 等分余数分摊 + use+=1 |
| `findById(Integer)` | —（只读） | 按id查询 + 状态实时判定 |
| `findForStudent(Integer)` | —（只读） | 学生视角预览（剔除答案判分字段） |
| `publish(Integer)` | ✅ `@Transactional` | draft→publish 状态机校验 |
| `withdraw(Integer)` | ✅ `@Transactional` | publish→draft 状态机校验 |
| `delete(Integer)` | ✅ `@Transactional` | 仅draft可删 状态机校验 |
| `findByStatus(ExamStatus)` | —（只读） | 按状态筛选 |
| `findAll()` | —（只读） | 全部考试列表 |
| `findAvailableForStudent()` | —（只读） | 学生可参加列表（publish/running） |
| `resolveCurrentStatus(Exam, LocalDateTime)` | —（只读） | 时间窗实时状态计算 |
| `weightedRandomPick(List, int)` | —（私有） | use降权加权随机算法 |
| `extractOptionsOnly(Question)` | —（私有） | 学生视图答案脱敏 |

### 🔴 发现的问题/缺失的功能

#### 问题 1：考试编辑功能完全缺失（❌ 高 — 必须修复）
- **现状**：`ExamService` 中**没有 `edit`/`update` 方法**
- **规范要求**：
  - M03-Exam-Assembly.md §1 — "编辑考试：**仅 draft 状态**可改"
  - M03-Exam-Assembly.md §7 业务规则1 — "草稿可改、发布后不可改"
  - M03-Exam-Assembly.md §6 API接口定义 — `PUT /api/exams/{id}` 明确要求编辑接口
- **影响**：**前端开发将无法实现草稿考试的编辑功能**，直接影响"组卷→预览→调整→发布"的完整工作流
- **业务影响分析**：教师组卷后若发现题目有误或需调整分值，无法修改只能删除重建，严重影响用户体验

**修改方案（待确认）**：

```java
// 需要新增的方法
@Transactional(rollbackFor = Exception.class)
public ExamVO edit(Integer id, ExamCreateManualReq req) {
    Exam exam = examRepository.findById(id)
        .orElseThrow(() -> new BusinessException(4301, "考试不存在"));
    
    // 1. 仅 draft 状态可编辑
    if (exam.getStatus() != ExamStatus.draft) {
        throw new BusinessException(4303, "仅草稿状态可编辑");
    }
    
    // 2. 校验时间
    validateTimeWindow(req.starttime(), req.endtime());
    
    // 3. 校验所有 questionId 存在
    List<Integer> questionIds = req.items().stream().map(ExamQuestionItemReq::questionId).toList();
    List<Question> questions = questionRepository.findAllById(questionIds);
    if (questions.size() != questionIds.size()) {
        throw new BusinessException(4301, "部分题目不存在");
    }
    
    // 4. 构造新的 question_sum 快照
    Map<Integer, Question> qMap = questions.stream()
        .collect(Collectors.toMap(Question::getId, q -> q));
    List<QuestionSumItem> items = req.items().stream()
        .map(item -> new QuestionSumItem(
            item.questionId(), item.score(),
            qMap.get(item.questionId()).getType()
        ))
        .toList();
    int totalScore = items.stream().mapToInt(QuestionSumItem::score).sum();
    QuestionSum questionSum = new QuestionSum(1, items, items.size(), totalScore);
    
    // 5. 处理 use 统计：旧题目 decrement，新题目 increment
    QuestionSum oldSum = parseQuestionSum(exam.getQuestionSum());
    Set<Integer> oldQuestionIds = oldSum.items().stream()
        .map(QuestionSumItem::questionId).collect(Collectors.toSet());
    Set<Integer> newQuestionIds = items.stream()
        .map(QuestionSumItem::questionId).collect(Collectors.toSet());
    
    // 移除的题目 use -= 1
    for (Integer qId : oldQuestionIds) {
        if (!newQuestionIds.contains(qId)) {
            questionService.decrementUse(qId);
        }
    }
    // 新增的题目 use += 1
    for (Integer qId : newQuestionIds) {
        if (!oldQuestionIds.contains(qId)) {
            questionService.incrementUse(qId);
        }
    }
    // 题目分值变化不影响 use
    
    // 6. 更新 exam
    exam.setExam(req.exam());
    exam.setStarttime(req.starttime());
    exam.setEndtime(req.endtime());
    exam.setQuestionSum(toJson(questionSum));
    Exam saved = examRepository.save(exam);
    
    return toVO(saved);
}
```

**同时需要在 QuestionService 中新增 `decrementUse()` 方法**：

```java
@Transactional(rollbackFor = Exception.class)
public void decrementUse(Integer questionId) {
    int updated = questionRepository.decrementUse(questionId);
    if (updated == 0) {
        throw new BusinessException(4201, "题目不存在，无法更新统计");
    }
}
```

**在 QuestionRepository 中新增**：

```java
@Modifying
@Query("UPDATE Question q SET q.use = q.use - 1 WHERE q.id = :id AND q.use > 0")
int decrementUse(@Param("id") Integer id);
```

---

## 5. M04 分数与统计模块审查结果：✅ 完全具备

### 已具备的功能函数（ScoreService.java）

| 方法签名 | 事务控制 | 核心逻辑 |
|----------|---------|----------|
| `submitExam(ExamSubmitReq, Integer)` | ✅ `@Transactional` | running状态校验 + 重复提交拒绝 + 批量加载question + 逐题判分 + score持久化 + question.correct+=1 |
| `gradeEssay(Integer, EssayGradeReq)` | ✅ `@Transactional` | 查找score记录 + 更新detail + 重算总分 + question.correct条件增量 |
| `findByUser(Integer)` | —（只读） | 个人所有成绩查询 |
| `findByExam(Integer)` | —（只读） | 某考试所有考生成绩 |
| `findById(Integer)` | —（只读） | 分数详情 |
| `findMistakes(Integer)` | —（只读） | 错题集（LinkedHashMap去重） |
| `getExamStatistics(Integer)` | —（只读） | 考试统计（通过率/平均分/分数分布/中位数） |
| `getQuestionStatistics()` | —（只读） | 题目统计（use/correct/accuracy） |
| `gradeOne(Question, Object, int)` | —（私有） | 5种题型判分逻辑（switch pattern matching） |
| `extractCorrectAnswer(Question)` | —（私有） | 从answer JSON提取标准答案 |

### 发现的问题/缺失的功能

#### 问题 1：自动保存机制缺失（ℹ️ 信息性）
- **现状**：无 `saveDraft()` 或 `autoSave()` 方法
- **审查要求提及**："是否有接收前端高频'自动保存'作答记录的接口？"
- **实际情况**：当前设计为"一次性提交"模式，无中途保存机制
- **审查结论**：**非硬性缺失**。Wiki 文档未定义自动保存功能，`ExamSubmitReq` 仅定义最终交卷。若前端需要自动保存功能，需新增业务需求并更新 Wiki

#### 问题 2：ScoreVO 中 userName 字段为 null（⚠️ 低）
- **位置**：`toScoreVO()` 方法第457行 `null, // userName 需跨模块查 user 表，Controller 层补充`
- **影响**：Service 层未实现跨模块查询，依赖 Controller 层补充
- **建议**：在 Service 层注入 UserRepository 补全 userName，保持 Service 层完整性

---

## 6. 跨模块审查结果

### 6.1 模块依赖关系
```
M01 UserService (无外部依赖)
    ↓
M02 QuestionService (无外部依赖)
    ↓
M03 ExamService → 依赖 QuestionService (incrementUse)
    ↓
M04 ScoreService → 依赖 ExamService (resolveCurrentStatus) + QuestionService (incrementCorrect)
```
- ✅ 依赖方向合理，无循环依赖
- ✅ 跨模块调用通过 Service 接口，未直接操作 Repository

### 6.2 事务边界审查
- ✅ 所有写操作方法均标注 `@Transactional(rollbackFor = Exception.class)`
- ✅ 组卷事务（INSERT exam + UPDATE question.use）在同一事务内
- ✅ 判分事务（UPSERT score + UPDATE question.correct）在同一事务内

### 6.3 N+1 查询防护
- ✅ ExamService：`questionRepository.findAllById(questionIds)` 批量加载
- ✅ ScoreService：`questionRepository.findAllById(questionIds)` 批量加载
- ✅ ScoreService.findByUser/findByExam/findById：均使用 findAllById

### 6.4 Entity/DTO 隔离
- ✅ 所有 Service 返回 VO/DTO，无 Entity 直接暴露
- ✅ 转换方法均为 private（toVO/toScoreVO）

---

## 7. 单元测试覆盖度

| 模块 | 测试类 | 测试方法数 | 状态 |
|------|--------|-----------|------|
| M01 UserService | UserServiceTest.java | 12 | ✅ 全部通过 |
| M02 QuestionService | QuestionServiceTest.java | 15 | ✅ 全部通过 |
| M03 ExamService | ExamServiceTest.java | 13 | ✅ 全部通过 |
| M04 ScoreService | ScoreServiceTest.java | 11 | ✅ 全部通过 |
| **总计** | | **73** | ✅ 全部通过 |

---

## 8. 总结与行动建议

### 8.1 各模块总体评估

| 模块 | 评级 | 核心功能完整度 | 主要缺口 |
|------|------|---------------|----------|
| 公共基础设施 | ✅ 完全具备 | 100% | 无 |
| M01 用户认证 | ⚠️ 部分具备 | 85% | 删除级联检查 TODO、注册角色限制 |
| M02 题库管理 | ⚠️ 部分具备 | 90% | 批量删除、删除引用提示 |
| **M03 考试组卷** | **❌ 严重缺失** | **75%** | **编辑功能完全缺失（高优先级）** |
| M04 分数统计 | ✅ 完全具备 | 95% | 自动保存（非必需）、userName补全 |

### 8.2 建议执行顺序（优先级排序）

1. **🔴 立即执行**：M03 考试编辑功能（影响前端开发阻塞）
   - 新增 `ExamService.edit()` 方法
   - 新增 `QuestionService.decrementUse()` 方法
   - 新增 `QuestionRepository.decrementUse()` JPQL
   - 编写对应单元测试

2. **🟡 近期执行**：M01 删除级联检查
   - `UserService.delete()` 中注入 ScoreRepository 检查关联数据

3. **🟢 远期优化**：
   - M01 注册角色限制
   - M04 ScoreVO 补全 userName
   - M02 批量删除功能
